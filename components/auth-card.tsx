import Link from "next/link";
import { Input } from "@/components/forms";

export function AuthCard({ title, text, footer }: { title: string; text: string; footer: React.ReactNode }) {
  return <div className="grid gap-8 lg:grid-cols-[0.85fr_1.15fr]"><div className="rounded-[2rem] bg-brand p-8 text-white shadow-glow"><span className="inline-flex rounded-full border border-white/20 px-3 py-1 text-xs uppercase tracking-[0.2em] text-white/70">Secure access</span><h1 className="mt-5 text-3xl font-semibold">{title}</h1><p className="mt-4 max-w-md leading-7 text-white/80">{text}</p><div className="mt-8 space-y-3">{["Role-based access control", "Audit-ready workflows", "Candidate-safe experience"].map((item) => <div key={item} className="rounded-2xl bg-white/10 px-4 py-3 text-sm backdrop-blur">{item}</div>)}</div></div><div className="panel p-8"><div className="space-y-4"><Input placeholder="Work email" /><Input type="password" placeholder="Password" /><Input placeholder="Optional company or candidate ID" /><button className="w-full rounded-full bg-brand px-5 py-3 text-sm font-semibold text-white">Continue</button></div><div className="mt-4 text-sm text-muted">{footer}</div><div className="mt-6 text-sm text-muted"><Link href="/auth/forgot-password" className="font-medium text-primary">Forgot password?</Link></div></div></div>;
}