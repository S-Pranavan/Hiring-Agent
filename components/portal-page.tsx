import { MetricCard } from "@/components/metric-card";
import { Metric, Role } from "@/lib/types";

export function PortalPage({ role, title, description, metrics, children }: Readonly<{ role: Role; title: string; description: string; metrics: Metric[]; children: React.ReactNode }>) {
  return <div className="space-y-8"><section className="panel-3d overflow-hidden p-6 lg:p-8"><div className="grid gap-6 lg:grid-cols-[1.2fr_0.8fr]"><div><span className="inline-flex rounded-full bg-blue-50 px-4 py-2 text-sm font-medium text-primary">{role === "admin" ? "Operations intelligence" : role === "candidate" ? "Personal progress" : "Hiring collaboration"}</span><h1 className="mt-4 text-3xl font-semibold tracking-tight text-ink sm:text-4xl">{title}</h1><p className="mt-4 max-w-2xl text-base leading-7 text-muted">{description}</p></div><div className="rounded-[2rem] bg-brand p-6 text-white shadow-glow"><p className="text-sm text-white/75">Live AI workflow health</p><div className="mt-5 space-y-4">{["Scoring pipeline synced", "Interview evidence indexed", "Decision audit trail ready"].map((item) => <div key={item} className="rounded-2xl bg-white/12 px-4 py-3 text-sm backdrop-blur">{item}</div>)}</div></div></div></section><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{metrics.map((metric) => <MetricCard key={metric.label} metric={metric} />)}</section>{children}</div>;
}